## 변수와 환경 변수
: 변수와 환경 변수에 설정된 값을 보기 위한 명령어에는 set, env가 있음

# set
: shell 변수와 환경 변수 값을 모두 볼 수 있음
: ex) set | more

# env
: 환경 변수만 볼 수 있음

# export
: 환경 변수 등록

# unset
: 환경 변수 해제


## 주요 환경 변수
: unix 시스템이 사용하는 변수

# HOME
: 사용자 홈 디렉토리의 절대 경로

# LOGNAME
: 사용자 계정 이름

# PATH
: 명령을 탐색할 경로

# CDPATH
: cd 명령어 사용 시 인자로 주어진 디렉터리를 찾을 경로


## Alias
: 긴 명령어를 짧게 만들어 사용하는데 이용됨
: ex) alias ls=’ls –F’


## History
#.bash_history
: 최근에 사용한 명령어가 저장된 파일

# fc –l
: 최근에 사용한 명령어 16개를 보여줌

# !!
: 바로 직전에 수행한 명령어 실행

# !
: 이전에 사용한 명령어 실행
: ex) !661 // 661 = 명령어 번호
